{
  "name": "cendere",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build && node tools/prune-dist.mjs",
    "preview": "vite preview --host",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "devDependencies": {
    "eslint": "^9.14.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
